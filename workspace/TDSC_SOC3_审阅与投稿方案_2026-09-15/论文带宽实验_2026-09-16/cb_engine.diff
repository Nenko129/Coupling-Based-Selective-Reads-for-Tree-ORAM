--- heterogeneous_oram.py
+++ cb_oram.py
@@ -90,19 +90,22 @@
 
 @dataclass(frozen=True)
 class Config:
-    kind:str; L:int; N:int; B:int=4096; Z:int=4; A:int=3; S:int=3; R:int=192; tree:int=0; compact:bool=True; fused:bool=True; Z_by_depth:tuple=(); S_by_depth:tuple=()
+    kind:str; L:int; N:int; B:int=4096; Z:int=4; A:int=3; S:int=3; R:int=192; tree:int=0; compact:bool=True; fused:bool=True; Z_by_depth:tuple=(); S_by_depth:tuple=(); Y:int=0; Y_by_depth:tuple=()
     def __post_init__(self):
+        if not self.ring or not self.fused:raise ValueError('CB engine requires a fused Ring-family protocol')
+        if not 0<=self.Y<=min(self.Z,self.S):raise ValueError('CB overlap')
         if self.kind not in KINDS or not(1<=self.L<=30 and 1<=self.Z<=8 and self.A>=1):raise ValueError('unsupported configuration')
         if self.N<1 or self.N>self.A*(1<<(self.L-1)) or self.B<4 or self.B%4:raise ValueError('N/geometry/block size')
         if self.n>64 or self.R<0 or (self.ring and self.S<1):raise ValueError('slots/stash/dummies')
-        for profile in (self.Z_by_depth,self.S_by_depth):
+        for profile in (self.Z_by_depth,self.S_by_depth,self.Y_by_depth):
             if profile and len(profile)!=self.L+1:raise ValueError('depth profile length')
         for d in range(self.L+1):
             z=self.Z_by_depth[d] if self.Z_by_depth else self.Z
             ss=self.S_by_depth[d] if self.S_by_depth else self.S
-            if not 1<=z<=8 or (self.ring and ss<1) or z+(ss if self.ring else 0)>64:raise ValueError('depth profile capacity')
+            y=self.Y_by_depth[d] if self.Y_by_depth else self.Y
+            if not 1<=z<=8 or ss<1 or not 0<=y<=min(z,ss) or z+ss-y>64:raise ValueError('depth profile capacity/overlap')
     def at(self,b):
-        return BucketConfig(self,depth(b)) if self.Z_by_depth or self.S_by_depth else self
+        return BucketConfig(self,depth(b)) if self.Z_by_depth or self.S_by_depth or self.Y_by_depth else self
 
     @property
     def ring(self):return self.kind in {'ring','gc_ring','r0'}
@@ -113,20 +116,18 @@
     @property
     def constrained(self):return self.selective
     @property
-    def n(self):return self.Z+self.S if self.ring else self.Z
+    def n(self):return self.Z+self.S-self.Y if self.ring else self.Z
     @property
     def levels(self):return tuple(range(1 if self.rootless else 0,self.L+1))
     @property
-    def secret(self):return ((8+(30 if self.compact else 64)*self.Z+31)//32)*32
+    def secret(self):return ((8+(8 if self.Y else 0)+(30 if self.compact else 64)*self.Z+31)//32)*32
     @property
     def H(self):return PUBLIC+NONCE+self.secret
     @property
     def W(self):return self.B+NONCE
     def context(self):
         base=encode(b'Selective-ORAM-optimization-v3',self.kind.encode(),*[ui(v,8) for v in (self.tree,self.L,self.N,self.B,self.Z,self.A,self.S,self.R,int(self.compact),int(self.fused))])
-        if self.Z_by_depth or self.S_by_depth:
-            return encode(b'SOC3-DEPTH-PROFILES-EVAL-v1',base,bytes(self.Z_by_depth),bytes(self.S_by_depth))
-        return base
+        return encode(b'SOC3-CB-EXPERIMENT-v1',base,bytes(self.Z_by_depth),bytes(self.S_by_depth),ui(self.Y,8),bytes(self.Y_by_depth))
 
 class BucketConfig:
     """Public per-bucket layout. All crypto uses the full profile context."""
@@ -137,9 +138,11 @@
     @property
     def S(self):return self.parent.S_by_depth[self.d] if self.parent.S_by_depth else self.parent.S
     @property
-    def n(self):return self.Z+self.S if self.ring else self.Z
-    @property
-    def secret(self):return ((8+(30 if self.compact else 64)*self.Z+31)//32)*32
+    def Y(self):return self.parent.Y_by_depth[self.d] if self.parent.Y_by_depth else self.parent.Y
+    @property
+    def n(self):return self.Z+self.S-self.Y if self.ring else self.Z
+    @property
+    def secret(self):return ((8+(8 if self.Y else 0)+(30 if self.compact else 64)*self.Z+31)//32)*32
     @property
     def H(self):return PUBLIC+NONCE+self.secret
 
@@ -177,13 +180,15 @@
 class Header:
     gamma:int=0; w:int=0; v:int=0; count:int=0; used:int=0
     live:dict[int,Desc]=field(default_factory=dict) # descriptors indexed by physical slot, private
+    green:int=0 # non-target real blocks consumed in this bucket epoch, encrypted
     def public(self)->bytes:
         return ui(self.gamma)+ui(self.w)+ui(self.v)+ui(self.count,4)+ui(self.used,8)
     def secret_bytes(self,c:Config)->bytes:
         ds=[self.live[j] for j in sorted(self.live)]
         need(len(ds)<=c.Z,'header capacity')
         # Active bitmap indexes the fixed Z descriptor array, not physical slots.
-        raw=ui((1<<len(ds))-1,8)
+        need(0<=self.green<=c.Y,'green counter bounds')
+        raw=(ui(self.green,8) if c.Y else b'')+ui((1<<len(ds))-1,8)
         for d in ds:
             raw+=ui(d.uid)+ui(d.address,8)+ui(d.leaf,4)+ui(d.slot,2)+(bytes(34) if not c.compact else b'')
         raw+=bytes((30 if c.compact else 64)*(c.Z-len(ds)))
@@ -192,7 +197,9 @@
     def decode(cls,c:Config,pub:bytes,plain:bytes):
         need(len(pub)==PUBLIC and len(plain)==c.secret,'header size')
         q=Cursor(pub);h=cls(q.uint(16),q.uint(16),q.uint(16),q.uint(4),q.uint(8));q.end()
-        r=Cursor(plain);livebits=r.uint(8);need(livebits<(1<<c.Z),'descriptor bitmap')
+        r=Cursor(plain);h.green=r.uint(8) if c.Y else 0
+        need(0<=h.green<=min(c.Y,h.count),'green counter bounds')
+        livebits=r.uint(8);need(livebits<(1<<c.Z),'descriptor bitmap')
         for i in range(c.Z):
             d=r.take(30 if c.compact else 64)
             if (livebits>>i)&1:
@@ -322,6 +329,7 @@
         self.c=c;self.P=P;self.io=transport;self.ctx=c.context();self.root=b''
         self.t=0;self.g=0;self.uid=0;self.stash:dict[int,Record]={};self.max_stash=0
         self.empty_root=P.F(b'EMPTY_ROOT',self.ctx)
+        self.cb_metrics=Counter()
         self.observer=None # optional external test-only observer, not used in path decisions
     def oid(self,b:int,j:int|None=None):return (b'H'+ui(b,8)) if j is None else (b'D'+ui(b,8)+ui(j,2))
     def btag(self,b:int,hd:bytes,D:bytes):return self.P.F(b'BUCKET',self.ctx,ui(b,8),hd,D)
@@ -330,7 +338,7 @@
     def prepare(self,b:int,old:Header,records:list[Record]|None,gamma:int|None=None):
         c=self.c.at(b)
         if records is None:
-            h=Header(old.gamma,old.w,old.v+1,old.count,old.used,dict(old.live));slots=None;local=None
+            h=Header(old.gamma,old.w,old.v+1,old.count,old.used,dict(old.live),old.green);slots=None;local=None
         else:
             need(len(records)<=c.Z,'bucket over capacity')
             rank=self.P.draw(self.ctx,b'PERMUTE'+ui(b,8))%math.factorial(c.n)
@@ -398,6 +406,10 @@
         return View(leaf,ds,headers,wh,D,B,siblings,pt,plains)
     def read_slots(self,view:View,selection:dict[int,tuple[int,...]],stage:str):
         need(hmac.compare_digest(view.path_tags[1],self.root),'stale local-read view')
+        empty={b:{} for b,v in selection.items() if not v}
+        need(all(not view.headers[b].live for b in empty),'empty opening with live payload')
+        selection={b:v for b,v in selection.items() if v}
+        if not selection:return empty
         c=self.c;ds=tuple(sorted(depth(b) for b in selection));count=sum(map(len,selection.values()))
         proofcount=sum(len(witnesses(c.at(b).n,tuple(sorted(v)))) for b,v in selection.items())
         body=ui(view.leaf,8)+ui(bitmap(ds),8)+b''.join(ui(bitmap(selection[node(view.leaf,d,c.L)]),8) for d in ds)
@@ -412,7 +424,7 @@
             allcts[b]=cts
         q.end()
         # All local openings, including dummy slots, pass before any payload decrypts.
-        return {b:{j:self.P.decrypt(self.ctx,self.oid(b,j),ui(view.headers[b].w),ct) for j,ct in cts.items()} for b,cts in allcts.items()}
+        return empty | {b:{j:self.P.decrypt(self.ctx,self.oid(b,j),ui(view.headers[b].w),ct) for j,ct in cts.items()} for b,cts in allcts.items()}
     def write(self,view:View,changes:dict[int,tuple],stage:str):
         need(hmac.compare_digest(view.path_tags[1],self.root),'stale write view')
         c=self.c;ds=tuple(sorted(depth(b) for b in changes));full=[];serialized={};newB=dict(view.bucket_tags);authup=0;dataup=0
@@ -434,12 +446,15 @@
             if self.observer is not None:self.observer('header',self.c.tree,b,h)
     def subset(self,b:int,h:Header)->tuple[int,...]:
         c=self.c.at(b);real=set(h.live);unused=[j for j in range(c.n) if not(h.used>>j&1)];dummy=[j for j in unused if j not in real]
-        need(real<=set(unused),'remaining real not unread');k=c.Z-len(real)
+        need(real<=set(unused),'remaining real not unread');cover=min(c.Z,len(unused));k=cover-len(real)
+        need(0<=k<=len(dummy),'CB cover capacity')
+        self.cb_metrics['maintenance_cover_slots']+=cover
+        self.cb_metrics['zero_cover_buckets']+=int(cover==0)
         draw=self.P.draw(self.ctx,b'RMW_SUBSET'+ui(b,8));den=math.comb(len(dummy),k)
         extra=choose_rank(dummy,k,draw%den)
         return tuple(sorted(real|set(extra)))
     def fused_logical(self,view:View,address:int):
-        from heterogeneous_fusion import fused_logical
+        from cb_fusion import fused_logical
         return fused_logical(self,view,address)
     def neutral(self,view:View,b:int):
         h=view.headers[b];selection=self.subset(b,h);plains=self.read_slots(view,{b:selection},'early')
