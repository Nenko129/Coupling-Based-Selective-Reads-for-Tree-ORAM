# 执行衔接更新：第六版

目标保持active；继续第五版，不缩小最终交付。用户最新追问为“除ρ外通信是否都能节省20%以上”。已根据本地结果纠正：Freecursive约22%–25%；异质IR相对Deferred约19.1%–19.25%，相对Path约9.63%–9.80%；静态AB约24%–26%仅组件/少量重复，完整AB未定；ρ整体约8%–12%。新的带宽收益口径核对_20260916.md记录分母及证据范围，最终答复应简要说明。

## 本轮完成

1. 新压缩map跨时隙实现及其阶段检查已完成；最终阶段收据passed，6串行等价、48中断、10故障。新源dwb_compressed_frontend.py、ir_compressed_controller.py，未修改旧raw执行闭包。
2. 实际执行check_ir_compressed_controller.py，session61857已exit0：24集成配置、4最终数据故障及两类交错全部通过。results/ir_compressed_controller_checks.json已生成。32号文档记录机制、分账、范围与下一步。无新增性能结果或容量证书。
3. 新CompressedWriteback已单独分类group_reset，先前阶段检查在该最终版本上重跑通过。后续不得使用旧StagedWriteback统计来误记其重置为posmap。

## 当前活跃任务

本轮实际进程核验22128、47116、15408、19648、6200、27740、46020、50656均存活，含义及会话沿用第五版。现有core/B3/扩展/B4/IR队列未重启、未修改源。

AB新策略目标规模6格当前5格通过：uniform的Ring/GC-Ring/R0峰值6/4/5；bottomD0的Ring/GC-Ring峰值11/6。各完成4096读回。第六格需等待实际结果。实际46020仍运行六格检查；50656仍等待27740，本轮检查ab_dummy_first_periodic_plan.json尚不存在，不能声称主ABDF60已启动。旧失败保持不变。若门槛通过应读取新计划和实际子进程，再用新审计入口更新31号结果。

本轮没有重新运行IR周期/扩展/B2分析器，因此第五版报告数（IR27/60、B2 44/45等）只是最后已审计快照，不能冒充本轮实时完成数。只读核验冻结514文件上次通过，未修改冻结源。

## 紧接着应做

- 从通过的小域压缩控制器检查推进真实性能驱动/计划/审计/目标pilot，不停留在测试。候选是N4096/B64/X32/β14/PLB8/cache6/R480，相同12288周期、暖1536请求/12288槽、测6144/49152；压缩人口4229，初始化对齐8059。先复核这些参数和相同公共phase；不要把候选当已预声明计划。β4压力切片是否增加须明确且保留失败。
- 参考旧run_ir_periodic.py与analyze_ir_dwb.py的分账/时钟/答案摘要要求，新建独立文件；旧审计器硬编码raw人口4369，不能原样套用压缩人口。需逐阶段reset计数、终点pending描述符、合理固定公共窗口、dirty驻留与未完成记录。
- 继续AB门槛和真实队列；B2凑齐后完成独立导出。原生IR-Stash、AB green归约/DeadQ/后台语义、公平基线执行归约、主表图及最终全要求审计仍未完成。
- 当前无用户阻塞。本轮有实质进展，不标记blocked/complete。不重复交付已生成的校准PDF。所有旧执行闭包锁定继续适用；改动新压缩源时必须重建绑定的两份检查收据，并先确认无活跃使用者。

